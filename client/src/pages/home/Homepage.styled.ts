import styled from 'styled-components';

const Header = styled.header`
  padding: 2em;
`;

const SectionList = styled.div`
  margin-top: 1em;
  background-color: #141432;
  border-radius: 10px;
  padding: 2em;
  width: calc(100% - 2em);
`;

const Section = styled.section`
  margin-top: 1em;
  background-color: #141432;
  border-radius: 10px;
  padding: 2em;
  width: calc(100% - 2em);
`;

export { SectionList, Section, Header };
